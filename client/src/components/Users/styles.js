import { makeStyles } from "@material-ui/core";

export default makeStyles((theme) => ({
  img: { width: "100px", height: "100px", borderRadius: "50%" },

  container: {},
  progress: {
    position: "absolute",
    top: "50%",
  },
}));
